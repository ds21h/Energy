<Window x:Class="Energy.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
    xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
    xmlns:local="clr-namespace:Energy"
    mc:Ignorable="d"
    WindowState="Maximized"
    WindowStyle="SingleBorderWindow"
    Title="MainWindow"
    Closing="Window_Closing"
    Width="789"
    Height="600">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Menu Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2">
            <MenuItem Header="_Bestand">
                <MenuItem x:Name="mnuLoad" Header="_Laad Meterstanden" Click="mnuLoad_Click"/>
                <MenuItem x:Name="mnuExport" Header="_Exporteer" Click="mnuExport_Click"/>
            </MenuItem>
        </Menu>

        <StackPanel Orientation="Vertical" Grid.Row="1" Grid.Column="0">
            <ComboBox x:Name="cmbFile" HorizontalAlignment="Stretch" Margin="0,5,0,0" SelectionChanged="cmbFile_SelectionChanged">
            </ComboBox>
            <CheckBox x:Name="chkBusiness" Content="Zakelijk" HorizontalAlignment="Right" Margin="0,10,0,0" VerticalAlignment="Top" Checked="chkBusiness_CheckedChanged" Unchecked="chkBusiness_CheckedChanged"/>
            <Label Content="Contract" HorizontalAlignment="Left" Margin="0,5,0,0" VerticalAlignment="Top"/>
            <ComboBox x:Name="cmbProvider" HorizontalAlignment="Stretch" Margin="20,0,0,0" SelectionChanged="cmbProvider_SelectionChanged">
            </ComboBox>
            <Label Content="Batterij" HorizontalAlignment="Left" Margin="0,10,0,0" VerticalAlignment="Top"/>
            <xctk:IntegerUpDown x:Name="udBattery" Margin="20,0,0,0" AllowTextInput="False" DefaultValue="0" Maximum="20" Minimum="0" ValueChanged="udBattery_ValueChanged" />
            <Label Content="Aansluiting" HorizontalAlignment="Left" Margin="0,10,0,0" VerticalAlignment="Top"/>
            <ComboBox x:Name="cmbResort" HorizontalAlignment="Left" Margin="20,0,0,0" Width="250" SelectionChanged="cmbResort_SelectionChanged">
            </ComboBox>
            <Button x:Name="btnList" Content="Lijst" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Margin="0,40,0,0" VerticalAlignment="Top" Click="btnList_Click"/>
            <Button x:Name="btnGraph" Content="Grafiek" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Margin="0,5,0,0" VerticalAlignment="Top" Click="btnGraph_Click"/>
            <Button x:Name="btnProv" Content="Contracten" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Margin="0,5,0,0" VerticalAlignment="Top" Click="btnContract_Click"/>
            <Button x:Name="btnDayTotal" Content="DagTotaal" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Margin="0,5,0,0" VerticalAlignment="Top" Click="btnDayTotal_Click"/>
            <Button x:Name="btnMonthTotal" Content="MaandTotaal" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Margin="0,5,0,0" VerticalAlignment="Top" Click="btnMonthTotal_Click"/>
        </StackPanel>
        <Frame x:Name="frView" NavigationUIVisibility="Hidden" Grid.Row="1" Grid.Column="1"/>
    </Grid>
</Window>
